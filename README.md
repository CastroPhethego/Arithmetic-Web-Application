# Arithmetic-Web-Application
This is a simple web application that allows a user to enter 2 numbers and select an arithmetic sign to perform an arithmetic login. The web application will then return the total thereafter.
